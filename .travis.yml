language: python
services:
  - docker
python:
  - "3.7"
addons:
  apt:
    packages:
      - docker-ce
install:
  - make install
before_script:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - mkdir local_storage
script:
  - make test
  - docker version
  - docker-compose up
  - make test.integration